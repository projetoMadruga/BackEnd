# ===================================
# CONFIGURAÇÕES GERAIS DA APLICAÇÃO
# ===================================
spring.application.name=Senai

# ===================================
# CONFIGURAÇÕES DO BANCO DE DADOS
# ===================================
spring.datasource.url=jdbc:mysql://mysql-madrutech-madrutech.i.aivencloud.com:12151/ouvidoria?sslMode=REQUIRED
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_ZmSh-JPazEeNC3lRonT
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================
# CONFIGURAÇÕES DO JPA/HIBERNATE
# ===================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${FORMAT_SQL:true}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=true

# ===================================
# CONFIGURAÇÕES DE SEGURANÇA
# ===================================
# 2 horas em milissegundos (7200000ms)
api.security.token.secret=${JWT_SECRET:senha2259}
api.security.token.expiration=${JWT_EXPIRATION:7200000}

# ===================================
# CONFIGURAÇÕES DE LOGGING
# ===================================
logging.level.org.springframework.web=${LOG_WEB_LEVEL:INFO}
logging.level.org.hibernate.SQL=${LOG_SQL_LEVEL:INFO}
logging.level.org.springframework.security=DEBUG

# ===================================
# CONFIGURAÇÕES DE E-MAIL
# ===================================
# Servidor SMTP do Gmail
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:465}
spring.mail.username=senaiouvidoria@gmail.com
spring.mail.password=fyzd gctx iyst gmuc
spring.mail.protocol=smtp

# Configurações de segurança do SMTP
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=false
spring.mail.properties.mail.smtp.starttls.required=false
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=${MAIL_SSL_TRUST:smtp.gmail.com}

# Timeouts (em milissegundos)
spring.mail.properties.mail.smtp.connectiontimeout=50000
spring.mail.properties.mail.smtp.timeout=50000
spring.mail.properties.mail.smtp.writetimeout=50000

# Outras configurações de e-mail
# Testa conexão na inicialização
spring.mail.test-connection=${MAIL_TEST_CONNECTION:false}
# Ativa modo debug (desativar em produção)
spring.mail.debug=true
# Modo de simulação de e-mail
app.mail.simulate=false

# ===================================
# CONFIGURAÇÕES DE REDEFINIÇÃO DE SENHA
# ===================================
# URL base para o link de redefinição de senha (produção)
app.reset-password-url-base=https://ouvidoria-senai-e9brd8b7gbg2a3f6.brazilsouth-01.azurewebsites.net

# ===================================
# CONFIGURAÇÕES DE VALIDAÇÃO
# ===================================
# Domínios de e-mail permitidos para cadastro
app.allowed-email-domains=aluno.senai.br,docente.senai.br,portalsesisp.org.br,sp.senai.br

# ===================================
# CONFIGURAÇÕES DE USUÁRIOS ADMIN
# ===================================
# Senha padrão para usuários admin (se não for fornecida, será gerada automaticamente)
admin.senha-padrao=
